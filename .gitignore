.DS_Store
.cache/
# Ignore per-run benchmark outputs, but keep the rescued 2021-2023 history.
results/*
!results/history
node_modules/
_site/
CLAUDE.md
AGENTS.md
# Local unpublished Verdaccio builds for benchmarking — keep the folder, ignore the tarballs.
tarballs/*
!tarballs/README.md
# Local Claude Code skills — repo-local only, never committed.
.claude/skills/
# Optimization / handoff reports — local only, never committed (see .claude/skills/optimization-report).
optimization-reports/